{
  "name": "@marketgoo/ola-emails",
  "description": "Ola design system implemented for emails, using MJML",
  "version": "0.1.0",
  "scripts": {
    "start": "nodemon --exec gulp demo",
    "publish:demo": "gulp demo && gh-pages -d demo",
    "prettier": "prettier --single-quote 'components/*.js' tokens.js --tab-width 4 --print-width=120 --write"
  },
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^2.1.1",
    "gulp": "^4.0.2",
    "nodemon": "^1.19.4",
    "prettier": "^1.18.2"
  },
  "dependencies": {
    "mjml": "^4.5.1"
  },
  "nodemonConfig": {
    "watch": [
      "demo/",
      "components/"
    ],
    "ext": "js,mjml"
  }
}
